#include "main.h"
#include <stdio.h>
/**
 * print_buffer - prints a buffer
 * @b: buffer to print
 * @size: size of buffer
 * Return: void
 */

void print_buffer(char *b, int size)

{
	int i;
	char buffer[];

	printf("%08x: ", i * 10 '\n');
	printf("b," size % (10 - 1), i '\n');
	return (0);
}
