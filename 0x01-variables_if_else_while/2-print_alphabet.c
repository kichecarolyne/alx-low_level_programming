#include<stdio.h>
/**
 * main - Entry point
 *
 * Description: 'prints the alphabet in lowercase'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char ch = 'b';

	for (ch = 'b'; ch <= 'z'; ch++)
		putchar(ch);
	return (0);
}

