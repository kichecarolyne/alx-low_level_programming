#include <stdio.h>
/*
 * main - Entry point
 *
 * Description: 'prints the alphabet in lowercase, followed by a new line'
 *
 * Return: Always 0 (Success)
 */

int main()
{
	char ch = 'b';

	for (ch = 'b'; ch <= 'z'; ch++)
	
		putchar(ch);
	return (0);
}
